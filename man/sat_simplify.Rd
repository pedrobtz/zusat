% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/controls.R
\name{sat_simplify}
\alias{sat_simplify}
\title{Simplify a formula without solving it}
\usage{
sat_simplify(solver)
}
\arguments{
\item{solver}{A \code{zusat_solver}.}
}
\value{
One of \code{"sat"}, \code{"unsat"} or \code{"unknown"}; \code{"unknown"} is the usual
outcome and simply means simplification alone did not decide it.
}
\description{
Runs CaDiCaL's inprocessing -- elimination, subsumption, probing and the
rest -- without the search that \code{\link[=sat_solve]{sat_solve()}} would do. Occasionally this
settles the formula on its own, which is why it returns a status.
}
\details{
Worth doing before a long incremental session, or between rounds when many
clauses have been added.

Like \code{\link[=sat_solve]{sat_solve()}}, this consumes any assumptions and limits currently set.
}
\examples{
s <- sat_solver(list(c(1, 2), c(-1, 2), c(1, -2)))
sat_simplify(s)
sat_solve(s)
}
