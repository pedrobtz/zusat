% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dimacs.R
\name{read_dimacs}
\alias{read_dimacs}
\title{Read a formula from a DIMACS CNF file}
\usage{
read_dimacs(path)
}
\arguments{
\item{path}{Path to a \code{.cnf} file.}
}
\value{
A list of integer vectors, one per clause, suitable for
\code{\link[=sat_solve]{sat_solve()}} or \code{\link[=sat_solver]{sat_solver()}}.
}
\description{
DIMACS is the format every SAT solver and benchmark set speaks, so this is
usually how a real problem arrives.
}
\details{
The parser is deliberately lenient about the things files in the wild
actually do:
\itemize{
\item clauses may span several lines, and several clauses may share one;
\item the \verb{p cnf} header is not required, and its counts are not trusted --
plenty of files disagree with themselves, and the clauses are the truth;
\item a final clause missing its terminating \code{0} is still read;
\item a \verb{\%} line ends the file, which is what SATLIB benchmarks use. Without
this the \code{0} that follows it would be read as an empty clause and turn
every SATLIB instance unsatisfiable.
}
}
\examples{
f <- tempfile(fileext = ".cnf")
write_dimacs(list(c(1, 2), c(-1, 3)), f)
read_dimacs(f)
sat_solve(read_dimacs(f))
}
\seealso{
\code{\link[=write_dimacs]{write_dimacs()}}
}
