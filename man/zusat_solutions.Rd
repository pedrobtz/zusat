% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enumerate.R
\name{zusat_solutions}
\alias{zusat_solutions}
\title{The result of an enumeration}
\description{
\code{sat_solutions()} returns an object of class \code{zusat_solutions}. It is a
data frame in long form, one row per variable per solution:
}
\details{
\describe{
\item{solution}{integer, which solution the row belongs to}
\item{variable}{integer, the variable number}
\item{value}{logical, its value in that solution}
}

Long form rather than one row per solution because the variables enumerated
over are chosen at call time via \code{vars}, so a wide frame would have a shape
that changes with the arguments. Long form also groups and joins directly;
use \code{split(x, x$solution)} to iterate solution by solution.
}
\section{Attributes}{

\code{status}, \code{n_solutions} and \code{complete}. Read them with \code{\link[=sat_status]{sat_status()}},
\code{\link[=sat_n_solutions]{sat_n_solutions()}} and \code{\link[=sat_complete]{sat_complete()}}.

\code{\link[=sat_complete]{sat_complete()}} is the one that matters: an enumeration stopped at \code{limit}
looks exactly like an exhaustive one unless you ask.
}

\examples{
sols <- sat_solutions(list(c(1, 2)))
class(sols)
sat_complete(sols)
}
\seealso{
\code{\link[=sat_solutions]{sat_solutions()}}, \code{\link[=sat_complete]{sat_complete()}}
}
