% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solution.R
\name{zusat_solution}
\alias{zusat_solution}
\title{The result of a solve}
\description{
\code{sat_solve()} returns an object of class \code{zusat_solution}. It \emph{is} a data
frame, with one row per variable and columns:
}
\details{
\describe{
\item{variable}{integer, the variable number}
\item{value}{logical, its value in the model. \code{NA} marks a variable the
solver left unassigned because either polarity extends the model.}
}

The outcome is carried as an attribute rather than a column, so the object
stays type-stable: an unsatisfiable result is the same data frame with no
rows, not a different type. Calling code can therefore index a result
without first branching on what came back, and a result drops into dplyr,
ggplot2 or a join with no conversion.

Read the outcome with \code{\link[=sat_status]{sat_status()}} or \code{\link[=sat_is_sat]{sat_is_sat()}}, never by checking
\code{nrow()}: a satisfiable formula with no variables also has zero rows.
}
\section{Attributes}{

\code{status} (one of \code{"sat"}, \code{"unsat"}, \code{"unknown"}), \code{n_vars}, \code{n_clauses}
and \code{elapsed} seconds. Prefer the accessors over reading these directly.
}

\examples{
sol <- sat_solve(list(c(1, 2)))
class(sol)
sat_status(sol)
}
\seealso{
\code{\link[=sat_solve]{sat_solve()}}, \code{\link[=sat_status]{sat_status()}}, \code{\link[=sat_assignment]{sat_assignment()}}
}
