% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/controls.R
\name{sat_limit}
\alias{sat_limit}
\title{Bound how hard a solve may work}
\usage{
sat_limit(solver, name, value)
}
\arguments{
\item{solver}{A \code{zusat_solver}.}

\item{name}{One of \code{"conflicts"}, \code{"decisions"}, \code{"preprocessing"},
\code{"localsearch"}, \code{"ticks"} or \code{"terminate"}. \code{"conflicts"} is the usual
choice: it bounds search effort in the unit solver authors reason about,
and is roughly proportional to work done.}

\item{value}{Maximum for that measure. A negative value means no limit.}
}
\value{
\code{solver}, invisibly.
}
\description{
SAT is NP-complete, so an innocuous-looking formula can take longer than
the remaining age of the universe. A limit makes a solve give up instead,
returning \code{"unknown"} rather than an answer.
}
\details{
This is what you want before calling \code{\link[=sat_solve]{sat_solve()}} anywhere a hang is
unacceptable: inside a loop, a Shiny app, or a scheduled job.
}
\section{Limits last one solve}{


Like assumptions, limits are consumed by the next \code{\link[=sat_solve]{sat_solve()}} and are not
retained afterwards. Set them again before each call.
}

\examples{
s <- sat_solver(list(c(1, 2), c(-1, 2)))
sat_limit(s, "conflicts", 1000)
sat_solve(s)
}
\seealso{
\code{\link[=sat_solve]{sat_solve()}}, \code{\link[=sat_status]{sat_status()}}
}
