on:
  push:
    branches: [main, master]
  pull_request:

name: vendor

permissions: read-all

jobs:
  vendor:
    uses: pedrobtz/r-actions/.github/workflows/vendor.yml@v1
    with:
      vendor-dir: src/cadical
      verify: tools/vendor/verify
      must-update: |
        tools/vendor/manifest.tsv
        tools/vendor/checksums.sha256
